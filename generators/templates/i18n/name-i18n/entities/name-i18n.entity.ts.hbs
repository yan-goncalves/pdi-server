import { LOCALES } from '@constants/locales'
import { {{pascalCase (slice name)}}Model } from '@{{kebabCase name}}/entities/{{kebabCase (slice name)}}.entity'
import {
  Column,
  CreateDateColumn,
  Entity,
  Index,
  JoinColumn,
  ManyToOne,
  PrimaryGeneratedColumn,
  UpdateDateColumn
} from 'typeorm'

@Entity('{{snakeCase name}}_i18n')
@Index(['locale', '{{camelCase field}}'], { unique: true })
export class {{pascalCase (slice name)}}LocaleModel {
  @PrimaryGeneratedColumn()
  readonly id: number

  @Column({ enum: LOCALES, default: LOCALES.BR })
  locale: LOCALES

  @ManyToOne(() => {{pascalCase (slice name)}}Model, ({{camelCase (slice name)}}) => {{camelCase (slice name)}}.id)
  @JoinColumn({ name: 'id_{{snakeCase (slice name)}}' })
  {{camelCase (slice name)}}: {{pascalCase (slice name)}}Model

  @Column({ unique: true })
  {{camelCase field}}: string

  @CreateDateColumn({ name: 'created_at' })
  createdAt: Date

  @UpdateDateColumn({ name: 'updated_at' })
  updatedAt: Date
}
